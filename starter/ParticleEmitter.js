class ParticleEmitter {
    constructor(settings) {
        this.settings = settings;
    }

    /**
     * Draws all of the particles in the emitter, given a 2D drawing context
     */
    draw(context) {
    }

    /**
     * Updates all of the particles in the emitter
     */
    update() {
    }
}